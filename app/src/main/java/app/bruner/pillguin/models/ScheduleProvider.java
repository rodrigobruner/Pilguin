package app.bruner.pillguin.models;

import app.bruner.library.models.Schedule;

/**
 * Interface to provide a schedule interface
 */
public interface ScheduleProvider {
    Schedule getSchedule();
}
